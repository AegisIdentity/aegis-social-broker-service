server:
  port: 9105

spring:
  application:
    name: aegis-social-broker-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AEGIS_ISSUER:http://localhost:9000}

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
